# The web/API server runs as the user configured in /etc/default/pi-web
# (PI_WEB_USER, default pi-web). The launcher resolves the user and drops
# to it with setpriv before exec'ing node (systemd's User= cannot take its
# value from an environment file).
[Unit]
Description=PI WEB web/API server
After=network.target pi-web-sessiond.service

[Service]
Type=simple
WorkingDirectory=/opt/pi-web
ExecStart=/usr/lib/pi-web/pi-web-launch server
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
